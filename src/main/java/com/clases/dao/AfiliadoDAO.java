package com.clases.dao;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import com.clases.modelo.Afiliado;

/*public interface AfiliadoDAO {
	//Page<Afiliado>obtenerTodos(Pageable pageable);

	//Page<Afiliado> obtenerTodos(Pageable pageable, String numeroDocumento);

}*/
